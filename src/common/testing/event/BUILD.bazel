load("//bazel:pl_build_system.bzl", "pl_cc_library", "pl_cc_test")

package(default_visibility = ["//src:__subpackages__"])

pl_cc_library(
    name = "cc_library",
    srcs = glob(
        ["*.cc"],
        exclude = [
            "*_test.cc",
        ],
    ),
    hdrs = glob(["*.h"]),
    deps = ["//src/common/event:cc_library"],
)

pl_cc_test(
    name = "simulated_time_system_test",
    srcs = ["simulated_time_system_test.cc"],
    deps = [":cc_library"],
)
