#pragma once

/**
 * Library to work with system related function (config, proc, etc.)
 * This library is system dependent and only works on Linux.
 */

#include "src/common/system/config.h"  // IWYU pragma: export
