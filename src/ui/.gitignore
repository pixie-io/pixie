# For produced assets.
dist/*

# Coverage results.
coverage/

# For Jenkins test results.
junit.xml

# Yarn/npm files.
node_modules
yarn-error.log
.yarn/*
# !.yarn/cache # Either huge cache in repository OR slow fresh installs locally. Neither is great. Choosing the latter.
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
