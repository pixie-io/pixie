---
short: Pod, Endpoint pair with most HTTP Data Transferred
long: >
  Finds the endpoint on a specific Pod that passes the most HTTP Data. Optionally, you can uncomment a line to
  see a table summarizing data per service, endpoint pair.
