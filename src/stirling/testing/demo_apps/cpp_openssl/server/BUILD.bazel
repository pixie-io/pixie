load("//bazel:pl_build_system.bzl", "pl_exp_cc_binary")

pl_exp_cc_binary(
    name = "server",
    srcs = ["server.cc"],
    data = [
        "cpp-tls-server.pem",
        "cpp-tls-server-cert.pem",
    ],
    deps = [
        "@boringssl//:ssl",
    ],
)
