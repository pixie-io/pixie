src/ui/node_modules
vendor
third_party/bpftrace/build
# Ignore this because we want to import it as a nested bazel repo.
third_party/threadstacks
tools/chef/nodes
# To keep third party dependencies separate, privy is intentional setup as a separate bazel workspace
src/datagen/pii/privy
