apiVersion: batch/v1
kind: Job
metadata:
  name: cert-provisioner-job
spec:
  template:
    metadata:
      name: cert-provisioner-job
    spec:
      serviceAccountName: updater-service-account
      containers:
      - name: provisioner
        image: gcr.io/pixie-oss/pixie-dev-public/vizier/cert_provisioner_image:latest
        envFrom:
        - configMapRef:
            name: pl-cloud-config
        - configMapRef:
            name: pl-cluster-config
            optional: true
      restartPolicy: "Never"
  backoffLimit: 1
  parallelism: 1
  completions: 1
