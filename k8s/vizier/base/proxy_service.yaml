apiVersion: v1
kind: Service
metadata:
  name: vizier-proxy-service
spec:
  type: NodePort
  ports:
    - port: 443
      protocol: TCP
      targetPort: 55000
      name: tcp-https
    - port: 80
      protocol: TCP
      targetPort: 55001
      name: tcp-http
  selector:
    name: vizier-proxy
