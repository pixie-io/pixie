apiVersion: v1
kind: Service
metadata:
  name: vizier-metadata
spec:
  type: ClusterIP
  ports:
  - name: tcp-grpc
    port: 50400
    protocol: TCP
    targetPort: 50400
  selector:
    name: vizier-metadata
