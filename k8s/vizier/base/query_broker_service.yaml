apiVersion: v1
kind: Service
metadata:
  name: vizier-query-broker
spec:
  type: ClusterIP
  ports:
  - name: tcp-http2
    port: 50300
    protocol: TCP
    targetPort: 50300
  selector:
    name: vizier-query-broker
