---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vizier-metadata
spec:
  template:
    spec:
      initContainers:
      - $patch: delete
        name: etcd-wait
      containers:
      - name: app
        volumeMounts:
        - mountPath: "/metadata"
          name: metadata-volume
      volumes:
      - name: metadata-volume
        persistentVolumeClaim:
          claimName: metadata-pv-claim
