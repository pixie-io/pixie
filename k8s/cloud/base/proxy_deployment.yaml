apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: cloud-proxy
spec:
  selector:
    matchLabels:
      name: cloud-proxy-server
  template:
    metadata:
      labels:
        name: cloud-proxy-server
    spec:
      containers:
      - name: cloud-proxy-server
        image: gcr.io/pl-dev-infra/cloud/proxy_server_image:latest
        ports:
          - containerPort: 56000
        readinessProbe:
          httpGet:
            scheme: HTTPS
            path: /healthz
            port: 56000
        env:
          - name: PL_JWT_SIGNING_KEY
            valueFrom:
              secretKeyRef:
                name: pl-app-secrets
                key: jwt-signing-key
          - name: PL_TLS_CERT
            value: /certs/tls.crt
          - name: PL_TLS_KEY
            value: /certs/tls.key
          - name: PL_AUTH0_DOMAIN
            value: pixie-labs.auth0.com
          - name: PL_AUTH0_CLIENT_ID
            value: qaAfEHQT7mRt6W0gMd9mcQwNANz9kRup
        volumeMounts:
            - name: certs
              mountPath: /certs
      volumes:
        - name: certs
          secret:
            secretName: proxy-tls-certs
