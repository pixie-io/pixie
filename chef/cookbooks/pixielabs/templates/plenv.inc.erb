# DO NOT EDIT! This is an automatically generated file by Chef and may be changed.
export CLANG_PATH=/opt/clang-7.0
export PATH=/opt/pixielabs/bin:$PATH
export PATH=/opt/google-cloud-sdk/bin:$PATH
export PATH=/opt/node/bin:$PATH
export PATH=/opt/phab/arcanist/bin:$PATH
export PATH=/usr/lib/go-1.10/bin/:$PATH
export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:$PATH
export PATH=${CLANG_PATH}:$PATH

export LD_LIBRARY_PATH=${CLANG_PATH}/lib:${LD_LIBRARY_PATH}

export CC=clang
export CXX=clang++

export ASAN_SYMBOLIZER_PATH=/opt/clang-7.0/bin/llvm-symbolizer

if [ -n "`$SHELL -c 'echo $ZSH_VERSION'`" ]; then
   # Assume Zsh.
   source /opt/google-cloud-sdk/path.zsh.inc
   source /opt/google-cloud-sdk/completion.zsh.inc
elif [ -n "`$SHELL -c 'echo $BASH_VERSION'`" ]; then
   # Assume Bash.
   source /opt/google-cloud-sdk/path.bash.inc
   source /opt/google-cloud-sdk/completion.bash.inc
fi
