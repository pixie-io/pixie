apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: tags
spec:
  selector:
    matchLabels:
      app: tags
  replicas: 1
  template:
    metadata:
      labels:
        app: tags
    spec:
      containers:
      - env:
        - name: SHARELATEX_APP_NAME
          value: Our ShareLaTeX
        - name: SHARELATEX_MONGO_URL
          value: mongodb://mongo/sharelatex
        - name: REDIS_HOST
          value: "redis.default.svc.cluster.local"
        - name: REDIS_PORT
          value: "6379"
        image: gcr.io/pl-dev-infra/mic92/sharelatex-tags:latest
        name: tags
        ports:
        - containerPort: 3000
          protocol: TCP
      restartPolicy: Always
